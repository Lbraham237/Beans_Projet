# ── Dashboard (déploiement Streamlit Cloud) ────────────
numpy>=1.26
tensorflow-cpu>=2.15
streamlit>=1.30
pillow>=10.0

# ── Dev / Notebook (local uniquement) ─────────────────
# pip install -r requirements-dev.txt
